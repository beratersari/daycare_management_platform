/**
 * Props for the ParentDashboard component.
 * Currently accepts no props but defined for future extensibility.
 */
export interface ParentDashboardProps {
  // Reserved for future use (e.g., child filter, custom actions)
}
