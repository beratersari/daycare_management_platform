/**
 * Props for the TeacherDashboard component.
 * Currently accepts no props but defined for future extensibility.
 */
export interface TeacherDashboardProps {
  // Reserved for future use (e.g., class filter, custom actions)
}
