/**
 * Props for the AdminDashboard component.
 * Currently accepts no props but defined for future extensibility.
 */
export interface AdminDashboardProps {
  // Reserved for future use (e.g., school filter, custom actions)
}
