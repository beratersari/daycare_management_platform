import React from 'react';

/**
 * Props for the AuthTemplate component.
 */
export interface AuthTemplateProps {
  /** Content to render inside the template (typically a form) */
  children: React.ReactNode;
}
