import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    gap: 12,
  },
  profileContainer: {
    gap: 16,
  },
  avatarRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 16,
    marginBottom: 8,
  },
  nameSkeletons: {
    flex: 1,
    gap: 4,
  },
  section: {
    gap: 8,
  },
  buttonRow: {
    flexDirection: 'row',
    gap: 12,
  },
});
